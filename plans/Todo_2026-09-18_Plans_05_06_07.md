# To-do across plans 05, 06 and 07 — as of 18 September 2026, 23:0x (the user asked for the overview; kept as one dated list, updated by dated edits, not rewritten)

Conversation with the supervisor: **Monday 28 September 2026** (decision 44). Anchor run's REPORT.md ≈ 24 Sep 19:00. Hetzner CPX62 (77.42.67.27) runs corpus layer A until ≈ 19 Sep 14:00.

## Running now (nothing to do but read the alarms)
- Laptop: anchor run **pid 441 since the resume of 20 Sep 19:34** (M3 TZ cells, naphthalene cc-pVTZ tight; the first run, pid 452, died in the 19:27 reboot with mode 12 sealed). Mode 12 done and read (M3 loses on C–H oop); mode 22 ≈ 23 Sep 01:00; mode 31 + REPORT.md ≈ 25 Sep 00:00–02:00. Night watch and host guard armed. Nothing on the laptop before REPORT.md.
- Hetzner: corpus layer A, 39 molecules, 16 threads; alarm-only watchdog (Monitor). Laptop chain disarmed.

## 19 September (Saturday), in this order — the user's order of 18 Sep
*(12:4x: items 1 and 2 done — layer A fetched (45 done, 0 failed), first learning curve in the desk note of 18 Sep §6, X22 result note; item 3 done 12:41: X19a LOSE, no X19b, no plan 07; item 4 done as a negative: cell 4 (both tight pairs, 6-31G) and cell 3 at cc-pVDZ all OOM at 31.5 GB — g stays 6.04, the rest needs ≥ 64 GB; item 5 (limit request) with the user. Helsinki idle since 14:2x.)*
1. Fetch layer A from Hetzner (tar over SSH): `molecules/`, `manifest.csv`, `ledger.csv`; merge; `status.py`; commit. Then the first learning-curve read on layer A (label count, the largest unknown) — a script, numbers to the ledger, read against the expectations of `05/GoalGathering/notes/Desk_2026-09-18_Network_Architecture_and_Label_Count.md` §4 (per family, 5/10/20/40 molecules, three seeds, power-law exponent).
2. **X22** (plan 06): the pattern-count ladder of X21 on the 39 low-symmetry corpus Hessians — does 2k+1 stay linear without D2h symmetry? numpy only, seconds. Result note in plan 06 `experiments/`.
3. **X19a** (plan 06, decides plan 07): write `experiments/x19_tuned_functional.py`, smoke on water, run on Hetzner (ω scan + three cc-pVTZ Hessians of benzene). **X19b** (naphthalene ω transfer) only on an X19a win.
4. Then on Hetzner: M2a cell 4 (LNO at tight thresholds, 24 GB) → g at the plan's thresholds; then the cc-pVDZ g attempt (needs ≈ 28 GB; may not fit). Delete the server between tasks — billing runs while it exists.
5. Hetzner (21 Sep evening): the dedicated-vCPU limit is now 32 (one CCX53 fits), but the **server-count limit is 5 and full** (hel1-14 + four E6 shards) and a limit request is refused until the first paid invoice ("user not allowed to request limit increase"); the user waits. The original purpose (M2a cells 3/4) lapsed with M2b. Plan for the CCX53 when possible: (1) decision 45's four mode-12 points from a copy of the sealed anchor directory, in parallel with the anchor; (2) after Friday a DZ deck if the report says DZ carries a family; (3) M2 build after the 28th. Fallback without a new server: decision 45 on the first freed E6 shard (Wednesday, after fetch + merge) or on hel1-14 after the R0 deck (Thursday).

## 20 September (Sunday)
- Evening: mode 12 complete → `m3_even_odd_parts.py` on the TZ directory beside the DZ one: first DZ-vs-TZ reading (C–H out-of-plane). Ledger + reading copy §3.3/§8 dated note. **Done 18:5x: M3 loses on C–H oop (increment −8.1 vs benzene +7.9 cm⁻¹, sign flip; MP2 double-ζ out-of-plane pathology); anchor stays TZ for that family; `results_m1/M3_TZ_MODE12_READING_2026-09-20.md`.** After the anchor: no new local run until the user has relaunched the app.
- Blog post (the user, 18 Sep: wait for the first anchor verdict — Sunday evening, not Saturday as first written): the first anchor verdict, with 18 Sep folded in as one paragraph (the struck sentence, the calendar, Helsinki).

## Helsinki, 21 September (hel1-14, kept by the user)
- Benzene VPT2 reruns at step 0.20 (3- and 5-point psi4 Hessians): expected ≈ 15:30; then `qff_from_hessians.py` on each cache — the route-disagreement statistic decides whether the anharmonic shifts are usable.
- Benzene R0 diagonal deck (44 TZ tight energies, `--tag _r0diag`): expected ≈ Tue evening at 8 threads; resume at 16 threads once the VPT2 runs end; then `r0_diagonal_reading.py` → 30 corrected harmonic frequencies for the 28 Sep table.
- E6 `--retry-failed` when a shard is free (≈ Wed).
- **T2 read 13:1x: as predicted (median 0.1, breathing −16, pairs 0.1); write-up in the note.** Open: (i) the user's yes on "VPT2 on pyscf Hessians" for sheet 8 and the pipeline; (ii) **done 18:2x**: `qff_from_hessians.py` aligns its basis inside degenerate subspaces with the displacements; T2 maximum 46.8 → 0.9, cubic outlier 625 → 24; psi4 sets unchanged; (iii) T1 read 16:3x: as predicted (median 2.3, max 110; breathing −28). **T3 launched 17:4x (decision 46; pid 157770 on hel1-14, `results_vpt2/pyscf_hessians_d010_grid99_590`, log `pyscf_hessians_d010.log`; ≈ 3–4 h): when it holds 61 files run `qff_from_hessians.py <dir> --disp 0.10` and `route_noise_structure.py`; expected: degenerate residual gone, breathing shift between −16 and −28; then set the pipeline step. Purpose: bracket the step-size systematic between T1 (−28) and T2 (−16) on the breathing mode — on the user's word; pipeline step size follows from it.**
- **Pre-registered demonstration (21 Sep 08:2x, `probes/results_vpt2/PREREGISTRATION_2026-09-21_FD_noise_demonstration.md`):** when `cache_benzene_d020` has 61 Hessians (T1) and `pyscf_hessians_d005_grid99_590` has 61 (T2): `qff_from_hessians.py` on each, `route_noise_structure.py` on each npz, results into the note's last section, then §7 of the three-band note and the pipeline choice of the VPT2 route (analytic Hessians via pyscf if T2 is clean). The five-point rerun `_d020p5` was stopped (2 of 61); resume only if T1 is ambiguous.
- pyVPT2 upstream: **done 21 Sep 09:3x** — issue #57 and PR #58 at philipmnel/pyvpt2; watch for maintainer comments (all three PRs: pyscf-forge #212, #213, pyVPT2 #58). Note for a resume of the `_d020` run: its cache keys carry `findif__points: 3` explicitly (the form before the 09:2x fix); resume with the old keyword form or accept recomputation.
- Sheet 1 of the architecture still carries the pre-reboot anchor dates (mode 22 on 22 Sep, report 24 Sep); after the mode-22 reading, refresh to the measured dates (mode 22 ≈ 23 Sep, REPORT.md ≈ 25 Sep). Research sheets may hold data.

- **T3 read 21:2x: analytic Hessians at 0.10 = T2 to 0.1 cm⁻¹, route maximum 0.44; pipeline step 0.10 (decision 46 B closed).** Next on hel1-14: nothing queued until the R0 deck (Tuesday evening); the benzene anharmonic step is a settled recipe (`make_qff_displacements.py --disp 0.10` → `pyscf_hessians_for_qff.py` → `python -m dpir.qff --disp 0.10`).
- **E6 shard 1 (hel1-10) restarted 20:32 after the Hetzner node outage** with `--max-molecules 18` (31 done + 1 failed before); ≈ 1 h behind; the watcher's TARGET=50 still holds.

## 22 and 24 September
- 22 Sep evening: mode 22 reading. 24 Sep evening: mode 31, REPORT.md, the full M3 TZ verdict → decision on the cheap-basis route; reading copy numbers; blog.
- After REPORT.md the laptop's first job is **decision 45 (20 Sep): the mode-12 densification, four TZ points (q = ±0.25, ±0.75) via `--modes 12 --npts 9 --resume`, ≈ 2 days; dry-run first (only four new points may be computed)**; then σ per family into the error budget. Windows updates stay paused to 1 Oct.

## Reading copy and proposal package for 28 September (desk, any quiet hour)
- Tolerance and shape findings: dated note in §7 (evaluation design) added 19 Sep 13:2x; the copy never stated 0.5 cm⁻¹ as a goal, only as Pirali's term and P25's licence bar.
- Cover note rewritten against the current reading copy — **draft of 20 Sep 21:0x: `05/GoalGathering/notes/Cover_Note_Draft_2026-09-20.md` (for the student to edit; the 12 Sep draft kept as history)**; plan 06 annex final — **update of 20 Sep appended (items 9–12: X19a closed, X22 linear everywhere, g 6.04 and the M2 gate, lever G's first-family loss)**; README decisions 43 and 44 added 19 Sep 13:0x.
- Number audit with sources (`probes/check_reading_copy_numbers.py <copy> <sources…>`, from `probes/`, `PYTHONIOENCODING=utf-8`) — **run 20 Sep 20:5x with 178 sources: 3 residuals, all arithmetic or a model name; rerun once more on 27 Sep after the last edits.**
- The user's own items (were dated 25 Sep, now by 27 Sep): Maltseva 2016 reading, header institution/role, §1 review-history paragraph, the three bracketed markers.
- Papers: gates A–D and their conditions in `05/GoalGathering/notes/PI_Assessment_2026-09-19_When_a_Paper_Is_Justified.md`; prerequisite for gate A: read Mata & Werner 2006, Russ & Crawford 2004, Subotnik & Head-Gordon 2005 in full (PDFs via the supervisor; ask on the 28th at the latest). **20 Sep evening: the five PDFs arrived and are read (reading note `notes/Reading_Note_2026-09-20_Supervisor_PDFs_L1_Smoothness_and_Mackie.md`); gate A's claim reworded in the PI assessment. Desk follow-ups **done 20 Sep 19:5x**: reading copy §1/§3.1 rewritten; pyVPT2 does polyads for positions (no intensities, no eigenvector export — own patch after 28 Sep, software-changes ledger); Mackie's numbers in Lines to Beat §3; cover note two sentences.**
- Pilot-note inputs (§12 table): naphthalene rehearsal recovery, R0 pilot, canonical two-mode points, noise run — **inventoried 20 Sep 21:0x (`notes/Desk_2026-09-20_Pilot_Note_Inputs_Inventory.md`): rehearsal and benzene canonical lines exist; R0 deck and noise run do not; decision for the user: run decision 35 as written after the anchor, or read σ from the anchor's 15 points plus a 4-point densification.**

## Quality policy (decision 47, 21:0x) — standing

- Every ledger incident → a test, a tool or a dated reason within a week (Monday desk half-hour; table in `QUALITY_POLICY.md`). Open guards: (a) shared `patch_file` helper for the patch-script rule; (b) launch wrapper (compile-check, water smoke, memory estimate) **before the M2 build**; (c) cache-key test for the task-keyword rule when the VPT2 runner is promoted. 
- Next promotions, in order: `make_qff_displacements.py` (with the pyVPT2-geometry check as its test), `route_noise_structure.py`, then the label-factory pieces as they stabilise. Each through the checklist; second-reader review noted in the commit.
- CI is live on push (`plan05` workflow); if it goes red, nothing else is pushed until it is green.

## Udacity modules (desk pass 07:1x, 22 Sep) — the user asked; reminder scheduled Saturday 26 Sep 09:00

- **Done 22 Sep:** module 03 report rebuilt (31 columns); module 05 dated status note (README, PROVENANCE); module 02 report/provenance carry the rename of the draft repository (`ai-programming-foundations-project` → `draft-qm9-foundations-unsubmitted`); fresh-environment check of modules 02–04 launched on hel1-14 (`modules_fresh_check.log`).
- **The user (weekend):** own pass over 02/03/04; **decision: the rubric wants a repository named exactly `ai-programming-foundations-project` (≥ 2 branches) — build a submission copy of module 02 under that name, or ask Udacity about the monorepo**; Zenodo release of module 04's training table (before 30 Oct) and later the corpus; mapping pass 6 still parked; optionally archive the renamed draft.

## After 28 September
- **Embedding line, after the E-series of 19 Sep (E4 stands; E1/E1b/E2/E5/E5b lost on 45 molecules):** (i) BHHLYP − B3LYP proxy check on benzene/naphthalene (stage A data on disk) — is the ring correction non-local in the proxy?; (ii) **E6** (pre-registered 19 Sep 23:2x): layer A2 as a learning curve in data — phase 1 first 200 molecules on 4 rented machines (≈ €60, ~2.5 days), phase 2 the rest (≈ €160); runner `--shard`, `merge_shards.py` ready; awaiting the user's word; (iii) block-level targets everywhere (RECIPE amendment 19 Sep).
- **E6 phase 1 (four CPX62, layer A2, 50 per shard; started 19 Sep 23:3x):** watchdog alarm-only; on "PHASE 1 DONE" fetch the four shard directories, `merge_shards.py`, then the E6 curve per the pre-registration. Added 20 Sep 11:0x: one retry pass for failed molecules (first: acenaphthylene+ethynyl, optking did not converge in 50 steps) with `opt_coordinates cartesian` and `geom_maxiter 200` — a `--retry-failed` mode in `run_corpus.py`, smoke-tested on that molecule before it runs on anything else. Then the user decides on phase 2 (~670 molecules, ≈ €160) and on deleting the servers.
- **Learned-features network** (design note 19 Sep): read the equivariant-Hessian literature; pre-registration of Test 1; minimal pair-block model in PyTorch; Stage 1 on the proxy ΔH of the 45 (then layer A2) molecules — can start before the 28th on the laptop at low priority if a quiet day allows; Stage 2 after M2's first decks. E-series of 19 Sep lost across the board (ridge 17.6, molecule tokens no change, contrastive embedding 19.3, supervised encoder 19.0 on ring-ip (means of three seeds)): next E3 = DFT-only pretext (frequency/family/sign from raw atom fields, whole corpus), and BHHLYP − B3LYP as the non-locality check on benzene/naphthalene; Stage 1 (full proxy ΔH) is the data-rich form.
- **M2 build** (decision 43; pre-registration of 18 Sep with T-M2-1..3, float64, checkpointing, g_M2 predicted 2–4): starts on the user's word; Hetzner `qcad` env first. Two to three weeks.
- **(T) port** (decision 41): one to two weeks, acceptance tests first.
- Corpus layer B on Hetzner for the learning curve beyond layer A (tens of euros).
- Lean on a second small Hetzner server (CPX32/42): install elan + Mathlib cache; theorems T3a–T3c and the resolvent lemma still to be written. Not before the 28th.
- Snellius Small Compute application (free, needs the supervisor) for cc-pVTZ gradients if Hetzner's dedicated types stay blocked.
- **Further upstream contributions from the software ledger (the user, 21 Sep: quality control, then submit; parked here for cost):** (a) pyVPT2 checkpoint option (row 14): a `cache_dir`/`checkpoint` field on `VPTInput` so an interrupted `FD: HESSIAN` run resumes; our 79-line layer is the prototype; needs a psi4-backed test in their suite, ≈ half a day; second PR after the route-consistency one. (b) LNO per-fragment checkpoint (row 3): upstream would want a `chkfile` attribute on the kernel rather than our subclass — open as a design issue with a pointer to `probes/lno_checkpoint.py`, ≈ 1 h. (c) Mathlib greedy-colouring lemma (row 8): re-check `master` first (Loogle found nothing 21 Sep), Mathlib expects a Zulip thread before a PR and a `lake build` check of the file (cache download ≈ 30 min on a clean machine); plan 06 material, the user decides. (d) psi4 documentation: **not needed** — the manual's frequency page already says "Analytic hessians are only available for RHF and UHF. For all other methods, Frequencies will proceed through finite differences" (checked 21 Sep evening); the oversight was ours.
- **Gate E prerequisite (2):** the two-route diagnostic on naphthalene (97 analytic pyscf Hessians, ≈ 1–2 days on hel1-14 after the current jobs) and one four-ring PAH (≈ a week) at the PAHdb-anharmonic level as far as reproducible; only after T1/T2 are read.

## Plan 06 — standing dates
- 15 October: branch C review (stays open on g = 6.04; transfers through M2's own gate, rule 2 amended 18 Sep).
- 1 December: branch M review (needs an E1/E2 statement with a measured first point and a proof plan, or it becomes a reading list).
- Reading: Mata & Werner 2006 — **done 20 Sep (PDF from the supervisor, read in full; frozen domains for numerical Hessians are Molpro practice; our claim is the transport of LNO-type spaces).**

## Plan 07 — does not open
- X19a lost on 19 September (tuned functional enlarges the correction, ratio 1.54, sign flip); X19b not run; lead A recorded. Lead D (σ/π split) is answered by X18 and lives in plan 05's per-family design. No plan 07 unless a new measured fact reopens it.
