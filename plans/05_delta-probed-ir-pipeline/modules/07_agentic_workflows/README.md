# Module 07 — Design of Autonomous and Semi-Autonomous Agentic Workflows

**Status (25 September 2026, 18:1x):** first build done — `steward/` (LangGraph graph, closed allow-list, deterministic gate, replay tools, rule-table and LLM policies), `rules/rules_v1.json` (32 lessons), `scenarios/` (S1–S8 on today's real logs; 8/8 with the deterministic policy), `tests/` (11 green), `notebook/agentic_system.ipynb` (executed), `Agentic_AI_System_Design_Report.docx/.pdf`, `requirements.txt`; the LLM policy waits for the user's Anthropic key (`M07_LLM=1`). `RUBRIC_CHECKLIST_2026-09-25.md`. Earlier status: designed, not implemented. `DESIGN_2026-09-25.md`: the *run steward* — a single bounded agent that keeps a pre-registered computational campaign moving (status, read, launch under an allow-list with a dry-run gate, retry, escalate), with eight evaluation scenarios replayed from the real events of 25 September and the framework and endpoint decided on 5–6 September (LangGraph; Anthropic API, model id logged) and one open question (timing). Implementation after the 28th unless the answers arrive earlier; the agent is evaluated on replayed logs, never live, until the report is written.
