{
  "name": "spectrum-atlas",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Spectrum Atlas — the public front door of the Δ-probed IR pipeline (static site; data from ../export/out)",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 4321",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1 --port 4321",
    "check": "astro check"
  },
  "dependencies": {
    "3dmol": "^2.5.5",
    "astro": "^5.2.0"
  }
}
